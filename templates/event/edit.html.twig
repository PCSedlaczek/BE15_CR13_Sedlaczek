{% extends "base.html.twig" %}

{% block body %}
<div class="container">
  <h2 class="page-header">Edit Event</h2>

  {{form_start(form)}}
  {{form_widget(form)}}
  {{form_end(form)}}

  <a class="btn btn-warning" href="/">Back</a>
</div>
{% endblock %}